version: '3.8'

services:
  yogesh-chat:
    build: .
    container_name: yogesh-chat-app
    restart: always
    ports:
      - "3005:3005"
    environment:
      - PORT=3005
      - NODE_ENV=production
    volumes:
      - ./uploads:/app/uploads
      - ./chat.db:/app/chat.db
